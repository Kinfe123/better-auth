---
title: "Atlassian"
description: "Atlassian provider setup and usage."
order: 2
---

# Atlassian

The Atlassian provider lets your users sign in with their Atlassian account, granting access to Jira, Confluence, and other Atlassian products through OAuth 2.0.

## Get your credentials

1. Go to the [Atlassian Developer Console](https://developer.atlassian.com/console/myapps/) and click **Create new app**.
2. Fill out the app details.
3. Under OAuth 2.0, configure your redirect URI: `https://yourdomain.com/api/auth/callback/atlassian`.
4. Note your **Client ID** and **Client Secret**.

The default scopes are `read:jira-user` and `offline_access`. For additional scopes, refer to the [Atlassian OAuth documentation](https://developer.atlassian.com/cloud/confluence/oauth-2-3lo-apps/).

## Configure the provider

```ts
import { betterAuth } from "better-auth"

export const auth = betterAuth({
  socialProviders: {
    atlassian: {
      clientId: process.env.ATLASSIAN_CLIENT_ID as string,
      clientSecret: process.env.ATLASSIAN_CLIENT_SECRET as string,
    },
  },
})
```

## Sign in

```ts
import { authClient } from "@/lib/auth-client"

await authClient.signIn.social({ provider: "atlassian" })
```
