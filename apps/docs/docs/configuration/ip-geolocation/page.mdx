---
title: "IP Geolocation"
description: "The project supports IP geolocation or location-aware session/event metadata."
---

# IP Geolocation

The project supports IP geolocation or location-aware session/event metadata.
## What This Is
IP geolocation enriches security and activity views with approximate location context. It usually depends on a provider or database file, so this page separates required config, optional provider tokens, and privacy-sensitive behavior.
## Why It Matters
IP Geolocation matters because it is part of how Root turns repository behavior into a usable developer or operator workflow. This page gives readers the mental model first, then uses implementation evidence as supporting context.
## Core Concepts
- IP address
- geolocation provider
- GeoLite
- session metadata
- privacy
## Typical Workflow
1. Choose the supported geolocation provider or database source.
2. Configure any required token, endpoint, or local database path.
3. Use location data as supporting context rather than a definitive identity signal.
## Detailed Configuration Model
IP Geolocation controls behavior rather than UI copy. The important questions are what decision this configuration helps a developer make, what happens when it is absent, and what runtime behavior changes when it is enabled. This page keeps the mental model ahead of the raw files that mention the setting.

For Root, configuration usually answers one of four questions: who can access the product, where it is mounted, how it talks to auth or database state, and what optional capabilities are enabled. Each setting is documented in relation to one of those questions.

## Decision Points
- Is the setting required for every installation, or only for a specific framework, adapter, or deployment mode?
- Does the setting affect security, persistence, presentation, event tracking, or developer convenience?
- What does a developer verify after changing it?

## Success Criteria
- Readers know when to configure this area and when they can ignore it.
- Readers understand the consequence of a wrong value.
- Readers can connect the setting to a visible product behavior or runtime integration.
## Configuration Surface
Configuration is easiest to understand when each setting is tied to the behavior it controls. The tables below collect the relevant files and environment variables after the mental model, so readers can connect the concept to the concrete setup surface.
### Files
- `demo/electron/package.json` (package) - Package metadata, scripts, dependencies, and exports.
- `demo/expo/package.json` (package) - Package metadata, scripts, dependencies, and exports.
- `demo/expo/tailwind.config.js` (styling) - Styling and CSS pipeline configuration.
- `demo/nextjs/package.json` (package) - Package metadata, scripts, dependencies, and exports.
- `demo/nextjs/postcss.config.mjs` (styling) - Styling and CSS pipeline configuration.
- `demo/oidc-client/package.json` (package) - Package metadata, scripts, dependencies, and exports.
- `demo/oidc-client/tailwind.config.ts` (styling) - Styling and CSS pipeline configuration.
- `demo/stateless/package.json` (package) - Package metadata, scripts, dependencies, and exports.
- `demo/stateless/postcss.config.mjs` (styling) - Styling and CSS pipeline configuration.
- `demo/stateless/tailwind.config.ts` (styling) - Styling and CSS pipeline configuration.
- `e2e/adapter/package.json` (package) - Package metadata, scripts, dependencies, and exports.
- `e2e/adapter/test/adapter-factory/package.json` (package) - Package metadata, scripts, dependencies, and exports.
- `e2e/adapter/test/drizzle-adapter/package.json` (package) - Package metadata, scripts, dependencies, and exports.
- `e2e/adapter/test/kysely-adapter/package.json` (package) - Package metadata, scripts, dependencies, and exports.
- `e2e/adapter/test/memory-adapter/package.json` (package) - Package metadata, scripts, dependencies, and exports.
- `e2e/adapter/test/mongo-adapter/package.json` (package) - Package metadata, scripts, dependencies, and exports.
### Environment Variables
- `APPDATA`
- `AUTH_SECRET`
- `BETTER_AUTH_SECRET`
- `BETTER_AUTH_TELEMETRY_ENDPOINT`
- `DATABASE_URL`
- `NODE_ENV`
- `npm_config_user_agent`
## How to Reason About This Config
- Identify whether the option affects security, runtime mounting, data access, or presentation.
- Prefer examples that use the same names and defaults found in the repository evidence.
- Call out adapter-specific behavior instead of implying every setting applies to every framework.
## Validation Checklist
- Keep config examples aligned with source defaults and runtime validation.
- Mention whether a setting is required, optional, or only needed for a specific adapter.
- Update this page when config object names, env vars, or supported providers change.
## Operational Notes
- The concept comes before the file paths, so readers understand the workflow before they inspect implementation evidence.
- Examples stay grounded in detected commands, config names, import paths, or behavior from the repository.
- Maintainer details live in the reference section so the main page stays useful to product readers.
- Required settings, optional settings, defaults, and adapter-specific behavior are separated clearly.
- Security impact and validation expectations are called out before examples.
## Maintainer Reference
The sections above are the reader-facing explanation. The references below are for maintainers who need to verify or update the implementation behind this page.

### Implementation Map
- `demo/electron/package.json` - Evidence file that contributes to the implementation understanding for this page.
- `demo/expo/package.json` - Evidence file that contributes to the implementation understanding for this page.
- `demo/expo/tailwind.config.js` - Evidence file that contributes to the implementation understanding for this page.
- `demo/nextjs/package.json` - Evidence file that contributes to the implementation understanding for this page.
- `demo/nextjs/postcss.config.mjs` - Evidence file that contributes to the implementation understanding for this page.
- `demo/oidc-client/package.json` - Evidence file that contributes to the implementation understanding for this page.
- `packages/cli/src/commands/info.ts` - Evidence file that contributes to the implementation understanding for this page.
- `packages/core/src/utils/ip.test.ts` - Ip.Test utility. It supports the feature behind the scenes and is often where shared rules live.
- `packages/core/src/utils/ip.ts` - Ip utility. It supports the feature behind the scenes and is often where shared rules live.

### Source Details
- `packages/cli/src/commands/info.ts`: language TypeScript; exports `info`; env `NODE_ENV`, `npm_config_user_agent`; flags `--config`, `--copy`, `--cwd`, `--json`
- `packages/core/src/utils/ip.ts`: language TypeScript; exports `createRateLimitKey`, `isValidIP`, `normalizeIP`

### Evidence
- `demo/electron/package.json`
- `demo/expo/package.json`
- `demo/expo/tailwind.config.js`
- `demo/nextjs/package.json`
- `demo/nextjs/postcss.config.mjs`
- `demo/oidc-client/package.json`
- `package.json:knip`
- `package.json:typescript`
- `packages/cli/src/commands/info.ts`
- `packages/core/src/utils/ip.test.ts`
- `packages/core/src/utils/ip.ts`
- `README.md#about-the-project`
