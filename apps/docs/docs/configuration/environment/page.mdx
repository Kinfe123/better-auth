---
title: "Environment Variables"
description: "The repository reads environment variables or provides environment example files."
order: 80
---

# Environment Variables

The repository reads environment variables or provides environment example files.
## What This Is
Environment variable documentation explains which runtime values the project expects, where those values are read, what each variable controls, and what developers should verify before running previews, workers, or production deployments.
## Why It Matters
Environment Variables matters because it is part of how Root turns repository behavior into a usable developer or operator workflow. This page gives readers the mental model first, then uses implementation evidence as supporting context.
## Core Concepts
- environment variables
- runtime secrets
- configuration files
- deployment values
## Typical Workflow
1. Start from example environment files when they exist.
2. Cross-check each variable against the source file that reads it.
3. Separate required runtime values from optional provider, preview, or deployment values.
## Detailed Configuration Model
Environment Variables controls behavior rather than UI copy. The important questions are what decision this configuration helps a developer make, what happens when it is absent, and what runtime behavior changes when it is enabled. This page keeps the mental model ahead of the raw files that mention the setting.

For Root, configuration usually answers practical questions: who can access the product, where it is mounted, which runtime services it depends on, and what optional capabilities are enabled. Each setting is documented in relation to one of those questions.

## Decision Points
- Is the setting required for every installation, or only for a specific framework, adapter, or deployment mode?
- Does the setting affect security, persistence, presentation, integrations, or developer convenience?
- What does a developer verify after changing it?

## Success Criteria
- Readers know when to configure this area and when they can ignore it.
- Readers understand the consequence of a wrong value.
- Readers can connect the setting to a visible product behavior or runtime integration.
## Configuration Surface
Configuration is easiest to understand when each setting is tied to the behavior it controls. The tables below collect the relevant files and environment variables after the mental model, so readers can connect the concept to the concrete setup surface.
### Files
- `demo/expo/src/lib/auth.ts` (product) - Product runtime configuration.
- `demo/nextjs/lib/auth.ts` (product) - Product runtime configuration.
- `demo/stateless/src/lib/auth.ts` (product) - Product runtime configuration.
- `e2e/integration/solid-vinxi/src/lib/auth.ts` (product) - Product runtime configuration.
- `packages/better-auth/src/types/auth.ts` (product) - Product runtime configuration.
- `e2e/smoke/test/fixtures/cloudflare/drizzle.config.ts` (database) - Database schema or adapter configuration.
- `demo/electron/electron.vite.config.ts` (framework) - Vite application configuration.
- `demo/nextjs/next.config.ts` (framework) - Next.js application configuration.
- `demo/oidc-client/vite.config.ts` (framework) - Vite application configuration.
- `demo/stateless/next.config.ts` (framework) - Next.js application configuration.
- `e2e/integration/vanilla-node/vite.config.ts` (framework) - Vite application configuration.
- `e2e/smoke/test/fixtures/vite/vite.config.ts` (framework) - Vite application configuration.
- `docker-compose.yml` (deployment) - Container or Docker Compose setup.
- `e2e/smoke/test/fixtures/cloudflare/wrangler.json` (deployment) - Deployment platform configuration.
- `packages/core/src/social-providers/railway.ts` (deployment) - Deployment platform configuration.
- `packages/core/src/social-providers/vercel.ts` (deployment) - Deployment platform configuration.
### Environment Variables
- `APPDATA`
- `AUTH_SECRET`
- `BETTER_AUTH_SECRET`
- `BETTER_AUTH_TELEMETRY_ENDPOINT`
- `DATABASE_URL`
- `NODE_ENV`
- `npm_config_user_agent`
## How to Reason About This Config
- Identify whether the option affects security, runtime mounting, data access, or presentation.
- Prefer examples that use the same names and defaults found in the repository evidence.
- Call out adapter-specific behavior instead of implying every setting applies to every framework.
## Validation Checklist
- Keep config examples aligned with source defaults and runtime validation.
- Mention whether a setting is required, optional, or only needed for a specific adapter.
- Update this page when config object names, env vars, or supported providers change.
## Operational Notes
- The concept comes before the file paths, so readers understand the workflow before they inspect implementation evidence.
- Examples stay grounded in detected commands, config names, import paths, or behavior from the repository.
- Maintainer details live in the reference section so the main page stays useful to product readers.
- Required settings, optional settings, defaults, and adapter-specific behavior are separated clearly.
- Security impact and validation expectations are called out before examples.
## Maintainer Reference
The sections above are the reader-facing explanation. The references below are for maintainers who need to verify or update the implementation behind this page.

### Implementation Map
- `packages/cli/src/commands/ai.ts` - Evidence file that contributes to the implementation understanding for this page.
- `packages/cli/src/commands/info.ts` - Evidence file that contributes to the implementation understanding for this page.
- `packages/cli/src/commands/init/configs/databases.config.ts` - Evidence file that contributes to the implementation understanding for this page.
- `packages/core/src/env/env-impl.ts` - Evidence file that contributes to the implementation understanding for this page.
- `packages/core/src/types/init-options.ts` - Init Options type surface. It documents data shapes, provider contracts, or configuration objects.
- `packages/core/tsdown.config.ts` - Evidence file that contributes to the implementation understanding for this page.

### Source Details
- `packages/cli/src/commands/ai.ts`: language TypeScript; exports `ai`; env `APPDATA`; flags `--args`, `--capabilities`, `--help`, `--provider`, `--registry-url`, `--scope`
- `packages/cli/src/commands/info.ts`: language TypeScript; exports `info`; env `NODE_ENV`, `npm_config_user_agent`; flags `--config`, `--copy`, `--cwd`, `--json`
- `packages/cli/src/commands/init/configs/databases.config.ts`: language TypeScript; exports `DatabaseAdapter`, `databasesConfig`, `DatabasesConfig`
- `packages/core/src/env/env-impl.ts`: language TypeScript; exports `env`, `ENV`, `EnvObject`, `getBooleanEnvVar`, `getEnvVar`, `isDevelopment`; env `NODE_ENV`
- `packages/core/src/types/init-options.ts`: language TypeScript; exports `BaseURLConfig`, `BetterAuthAdvancedOptions`, `BetterAuthDBOptions`, `BetterAuthOptions`, `BetterAuthRateLimitOptions`, `BetterAuthRateLimitRule`
- `packages/core/tsdown.config.ts`: language TypeScript; env `BETTER_AUTH_TELEMETRY_ENDPOINT`

### Evidence
- `packages/cli/src/commands/ai.ts`
- `packages/cli/src/commands/info.ts`
- `packages/cli/src/commands/init/configs/databases.config.ts`
- `packages/core/src/env/env-impl.ts`
- `packages/core/src/types/init-options.ts`
- `packages/core/tsdown.config.ts`
