---
title: "Better Auth Infrastructure"
description: "Enterprise-grade dashboard, security, and managed services for Better Auth."
order: 1820
---

# Better Auth Infrastructure

Enterprise-grade dashboard, security, and managed services for Better Auth.
Better Auth Infrastructure is a paid service that extends your authentication system with a management dashboard,
abuse protection, transactional messaging, and enterprise features - all without managing additional infrastructure.

## Key Features


You're one plugin away from getting started:

```ts
import { betterAuth } from "better-auth";
import { dash } from "@better-auth/infra";
import { sentinel } from "@better-auth/infra";

export const auth = betterAuth({
  plugins: [dash()],
});
```

Check out our [Getting Started](/docs/infrastructure/getting-started) guide for more details.

## Next Steps
## Generated Context
This page is grounded in the repository's existing documentation at `docs/content/docs/infrastructure/introduction.mdx`, then placed into the generated Docs Cloud sidebar for Better Auth.
## Maintainer Notes
- Keep this page aligned with the source documentation when the upstream docs content changes.
- Add implementation-specific examples only when they can be verified from source, package exports, or existing docs.
