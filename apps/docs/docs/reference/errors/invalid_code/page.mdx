---
title: "invalid_code"
description: "The provided authentication code is invalid or expired."
order: 1640
---

# invalid_code

The provided authentication code is invalid or expired.
This error occurs when the authentication code is invalid, expired, or cannot be verified.

## How to fix

* Restart the authentication flow to generate a new code
* Ensure the code is used only once and within its valid time window
* Verify that the correct code is being sent to the callback endpoint
## Generated Context
This page is grounded in the repository's existing documentation at `docs/content/docs/reference/errors/invalid_code.mdx`, then placed into the generated Docs Cloud sidebar for Better Auth.
## Maintainer Notes
- Keep this page aligned with the source documentation when the upstream docs content changes.
- Add implementation-specific examples only when they can be verified from source, package exports, or existing docs.
