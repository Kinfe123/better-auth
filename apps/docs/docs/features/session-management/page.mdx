---
title: "Session Management"
description: "The project includes session, sign-in, authentication, or token workflows."
---

# Session Management

The project includes session, sign-in, authentication, or token workflows.
## What This Is
This area explains how the product observes active authentication state. It connects user identity, session creation, sign-in activity, cookies or tokens, and any security-sensitive controls exposed to administrators.

Repository context: Authentication in the TypeScript ecosystem is a half solved problem.  Other open source libraries often require a lot of additional code for anything beyond basic authentication.  Rather than just pushing third party services as the solution, I believe we can do better as a community—hence, Better Auth.
## Why It Matters
Session Management matters because it is part of how Root turns repository behavior into a usable developer or operator workflow. This page gives readers the mental model first, then uses implementation evidence as supporting context.
## Core Concepts
- sessions
- sign in
- tokens
- cookies
- auth state
## Typical Workflow
1. Review session records for a user or the whole workspace.
2. Use session metadata to understand device, location, or activity context when available.
3. Document any destructive session action with its security impact before exposing it to operators.
## Detailed Behavior
Session Management works as a product workflow with three layers. The first layer is the user-facing experience: what an operator sees, what they can search or inspect, and what actions they can take. The second layer is the domain model: which records, relationships, or configuration values the workflow depends on. The third layer is the safety model: which permissions, access rules, or validation steps prevent accidental or unauthorized changes.

For Root, this feature is easiest to understand from the operator's point of view. A reader can understand what problem this page solves, what state it reads or changes, and what a successful outcome looks like before they need to think about source files.

## Common Scenarios
- A new operator wants to understand what session management is for before using it in production.
- A developer is wiring the feature into a host application and needs to know which data, config, or permissions are involved.
- A maintainer changed the feature and needs to update docs without forcing readers to understand the source tree first.

## Success Criteria
- Readers can explain what session management does in plain language.
- Readers know the main objects or settings involved before they see any file paths.
- Readers know which actions are safe, which actions are destructive, and which actions depend on access control.
## How It Fits in the Product
Session Management is a product capability, not just a list of source files. The important story is the user-facing outcome, the data or configuration it depends on, and the guardrails that make the workflow safe to operate.
## Repository Notes
These notes come from the repository README so the page stays grounded in existing project language.

### Why Better Auth
Authentication in the TypeScript ecosystem is a half-solved problem. Other open-source libraries often require a lot of additional code for anything beyond basic authentication. Rather than just pushing third-party services as the solution, I believe we can do better as a community—hence, Better Auth.
## Operational Notes
- The concept comes before the file paths, so readers understand the workflow before they inspect implementation evidence.
- Examples stay grounded in detected commands, config names, import paths, or behavior from the repository.
- Maintainer details live in the reference section so the main page stays useful to product readers.
- User-facing state, available actions, empty/error states, and permission boundaries are part of the product behavior.
- The page explains what data the feature reads or changes before referencing source files.
## Maintainer Reference
The sections above are the reader-facing explanation. The references below are for maintainers who need to verify or update the implementation behind this page.

### Implementation Map
- `demo/expo/src/lib/auth.ts` - Runtime configuration file that controls how the product is mounted, secured, or connected to auth.
- `demo/nextjs/lib/auth.ts` - Runtime configuration file that controls how the product is mounted, secured, or connected to auth.
- `demo/stateless/src/lib/auth.ts` - Runtime configuration file that controls how the product is mounted, secured, or connected to auth.
- `e2e/integration/solid-vinxi/src/lib/auth.ts` - Runtime configuration file that controls how the product is mounted, secured, or connected to auth.
- `packages/better-auth/package.json` - Evidence file that contributes to the implementation understanding for this page.
- `packages/better-auth/src/types/auth.ts` - Auth type surface. It documents data shapes, provider contracts, or configuration objects.
- `packages/cli/src/commands/init/generate-auth-client.ts` - Evidence file that contributes to the implementation understanding for this page.
- `packages/cli/src/commands/init/generate-auth.ts` - Runtime configuration file that controls how the product is mounted, secured, or connected to auth.
- `packages/cli/src/commands/init/utility/auth-client-config.ts` - Evidence file that contributes to the implementation understanding for this page.
- `packages/cli/src/commands/init/utility/auth-config.test.ts` - Evidence file that contributes to the implementation understanding for this page.

### Source Details
- `packages/cli/src/commands/init/generate-auth-client.ts`: language TypeScript; exports `generateAuthClientConfigCode`
- `packages/cli/src/commands/init/generate-auth.ts`: language TypeScript; exports `BaseGetArgumentsOptions`, `generateAuthConfigCode`, `GenerateAuthFileOptions`, `GetArgumentsFn`, `GetArgumentsOptions`
- `packages/cli/src/commands/init/utility/auth-client-config.ts`: language TypeScript; exports `generateInnerAuthClientConfigCode`
- `packages/cli/src/commands/init/utility/auth-config.ts`: language TypeScript; exports `generateInnerAuthConfigCode`
- `packages/cli/src/commands/login.ts`: language TypeScript; exports `login`, `logout`
- `packages/cli/src/commands/mcp.ts`: language TypeScript; exports `mcp`; flags `--claude-code`, `--cursor`, `--manual`, `--open-code`
- `packages/cli/test/install-dependencies.test.ts`: language TypeScript; flags `--dev`, `--force`, `--optional`, `--peer`, `--save-catalog`, `--save-catalog-name`
- `packages/core/src/db/schema/session.ts`: language TypeScript; exports `BaseSession`, `Session`, `sessionSchema`

### Evidence
- `demo/expo/src/lib/auth.ts`
- `demo/nextjs/lib/auth.ts`
- `demo/stateless/src/lib/auth.ts`
- `e2e/integration/solid-vinxi/src/lib/auth.ts`
- `packages/better-auth/package.json`
- `packages/better-auth/src/types/auth.ts`
- `packages/cli/src/commands/init/generate-auth-client.ts`
- `packages/cli/src/commands/init/generate-auth.ts`
- `packages/cli/src/commands/init/utility/auth-client-config.ts`
- `packages/cli/src/commands/init/utility/auth-config.test.ts`
- `packages/cli/src/commands/init/utility/auth-config.ts`
- `packages/cli/src/commands/login.ts`
