---
title: "Developer Tools"
description: "The project includes developer-facing tools, generators, inspectors, or visual editors."
---

# Developer Tools

The project includes developer-facing tools, generators, inspectors, or visual editors.
## What This Is
This section explains the utilities that help developers inspect, configure, or generate parts of the product. These tools are not just UI screens; they are shortcuts for common setup, debugging, schema, email, or integration work.
## Why It Matters
Developer Tools matters because it is part of how Root turns repository behavior into a usable developer or operator workflow. This page gives readers the mental model first, then uses implementation evidence as supporting context.
## Core Concepts
- developer tools
- generators
- inspectors
- visual editors
- debug workflows
## Typical Workflow
1. Choose the tool that matches the setup or debugging task.
2. Review generated code or detected state before applying it to the host project.
3. Use tool output as a starting point, then validate it against the application's real configuration.
## Detailed Behavior
Developer Tools works as a product workflow with three layers. The first layer is the user-facing experience: what an operator sees, what they can search or inspect, and what actions they can take. The second layer is the domain model: which records, relationships, or configuration values the workflow depends on. The third layer is the safety model: which permissions, access rules, or validation steps prevent accidental or unauthorized changes.

For Root, this feature is easiest to understand from the operator's point of view. A reader can understand what problem this page solves, what state it reads or changes, and what a successful outcome looks like before they need to think about source files.

## Common Scenarios
- A new operator wants to understand what developer tools is for before using it in production.
- A developer is wiring the feature into a host application and needs to know which data, config, or permissions are involved.
- A maintainer changed the feature and needs to update docs without forcing readers to understand the source tree first.

## Success Criteria
- Readers can explain what developer tools does in plain language.
- Readers know the main objects or settings involved before they see any file paths.
- Readers know which actions are safe, which actions are destructive, and which actions depend on access control.
## How It Fits in the Product
Developer Tools is a product capability, not just a list of source files. The important story is the user-facing outcome, the data or configuration it depends on, and the guardrails that make the workflow safe to operate.
## Operational Notes
- The concept comes before the file paths, so readers understand the workflow before they inspect implementation evidence.
- Examples stay grounded in detected commands, config names, import paths, or behavior from the repository.
- Maintainer details live in the reference section so the main page stays useful to product readers.
- User-facing state, available actions, empty/error states, and permission boundaries are part of the product behavior.
- The page explains what data the feature reads or changes before referencing source files.
## Maintainer Reference
The sections above are the reader-facing explanation. The references below are for maintainers who need to verify or update the implementation behind this page.

### Implementation Map
- `demo/oidc-client/package.json` - Evidence file that contributes to the implementation understanding for this page.
- `demo/oidc-client/tailwind.config.ts` - Evidence file that contributes to the implementation understanding for this page.
- `demo/oidc-client/vite.config.ts` - Evidence file that contributes to the implementation understanding for this page.
- `packages/cli/package.json` - Evidence file that contributes to the implementation understanding for this page.
- `packages/cli/src/api.ts` - Evidence file that contributes to the implementation understanding for this page.
- `packages/cli/src/commands/ai.ts` - Evidence file that contributes to the implementation understanding for this page.
- `packages/cli/src/commands/generate.ts` - Evidence file that contributes to the implementation understanding for this page.
- `packages/cli/src/commands/info.ts` - Evidence file that contributes to the implementation understanding for this page.
- `packages/cli/src/commands/init/configs/databases.config.ts` - Evidence file that contributes to the implementation understanding for this page.
- `packages/cli/src/commands/init/configs/frameworks.config.ts` - Evidence file that contributes to the implementation understanding for this page.

### Source Details
- `packages/cli/src/api.ts`: language TypeScript; exports `adapters`, `DBAdapter`, `generateDrizzleSchema`, `generateKyselySchema`, `generatePrismaSchema`, `generateSchema`
- `packages/cli/src/commands/ai.ts`: language TypeScript; exports `ai`; env `APPDATA`; flags `--args`, `--capabilities`, `--help`, `--provider`, `--registry-url`, `--scope`
- `packages/cli/src/commands/generate.ts`: language TypeScript; exports `generate`; flags `--adapter`, `--config`, `--cwd`, `--dialect`, `--output`, `--y`
- `packages/cli/src/commands/info.ts`: language TypeScript; exports `info`; env `NODE_ENV`, `npm_config_user_agent`; flags `--config`, `--copy`, `--cwd`, `--json`
- `packages/cli/src/commands/init/configs/databases.config.ts`: language TypeScript; exports `DatabaseAdapter`, `databasesConfig`, `DatabasesConfig`
- `packages/cli/src/commands/init/configs/frameworks.config.ts`: language TypeScript; exports `Framework`, `FRAMEWORKS`
- `packages/cli/src/commands/init/configs/social-providers.config.ts`: language TypeScript; exports `ProviderConfig`, `ProviderOption`, `SOCIAL_PROVIDER_CONFIGS`, `SOCIAL_PROVIDERS`, `SocialProvider`
- `packages/cli/src/commands/init/configs/temp-plugins.config.ts`: language TypeScript; exports `Plugin`, `PluginConfig`, `PluginsConfig`, `tempPluginsConfig`

### Evidence
- `demo/oidc-client/package.json`
- `demo/oidc-client/tailwind.config.ts`
- `demo/oidc-client/vite.config.ts`
- `package.json:@arethetypeswrong/cli`
- `package.json:@changesets/cli`
- `packages/cli/package.json`
- `packages/cli/src/api.ts`
- `packages/cli/src/commands/ai.ts`
- `packages/cli/src/commands/generate.ts`
- `packages/cli/src/commands/info.ts`
- `packages/cli/src/commands/init/configs/databases.config.ts`
- `packages/cli/src/commands/init/configs/frameworks.config.ts`
