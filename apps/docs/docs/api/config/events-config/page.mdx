---
title: "Events Config"
description: "The source includes types or helpers for events config."
---

# Events Config

The source includes types or helpers for events config.
## What This Is
This API reference page explains the events config shape, how developers pass it to the package, and which related feature pages depend on those settings.
## Why It Matters
Events Config matters because it is part of how Root turns repository behavior into a usable developer or operator workflow. This page gives readers the mental model first, then uses implementation evidence as supporting context.
## Core Concepts
- Events Config
- configuration type
- runtime options
- validation
## Typical Workflow
1. Locate the configuration object in the host application.
2. Set only the options needed for the selected runtime and feature set.
3. Use the related feature docs to understand behavior controlled by each option.
## Detailed API Contract
Events Config is a developer contract, not just an exported name. The page explains what the API represents, what problem it solves, what a caller provides, what the package returns or performs, and how it relates to the surrounding runtime.

API pages are most useful when they explain the shape of responsibility. For an adapter, that means the host framework owns routing and the package supplies a handler. For configuration, it means the host app supplies options and the package changes runtime behavior. For utilities, it means the function supports a feature that needs to be understood before the utility is changed.

## Integration Questions
- What does the caller provide?
- What does the package create, mutate, or return?
- Which feature or configuration page explains the user-facing behavior behind this API?
- What needs to be tested after this API changes?

## Success Criteria
- Readers understand the API purpose before looking at source files.
- Readers know whether the API is public integration surface or internal support code.
- Readers know which runtime or feature depends on it.
## Import Surface
Use `@better-auth/root` as the package path indicated by package metadata or adapter file evidence.
## API Mental Model
Configuration APIs are the contract between the host application and the product runtime. The important details are what each option changes and which feature depends on it.
## Related Exports
- `ai`
- `apple`
- `AppleNonConformUser`
- `AppleOptions`
- `AppleProfile`
- `atlassian`
- `AtlassianOptions`
- `AtlassianProfile`
- `cognito`
- `CognitoOptions`
- `CognitoProfile`
- `discord`
## Operational Notes
- The concept comes before the file paths, so readers understand the workflow before they inspect implementation evidence.
- Examples stay grounded in detected commands, config names, import paths, or behavior from the repository.
- Maintainer details live in the reference section so the main page stays useful to product readers.
- Inputs, outputs, side effects, and ownership boundaries are described before exported names.
- The API connects back to feature or configuration pages that explain user-facing behavior.
## Maintainer Reference
The sections above are the reader-facing explanation. The references below are for maintainers who need to verify or update the implementation behind this page.

### Implementation Map
- `packages/cli/src/commands/ai.ts` - Evidence file that contributes to the implementation understanding for this page.
- `packages/cli/src/commands/init/configs/social-providers.config.ts` - Evidence file that contributes to the implementation understanding for this page.
- `packages/core/src/oauth2/oauth-provider.ts` - Evidence file that contributes to the implementation understanding for this page.
- `packages/core/src/social-providers/apple.ts` - Evidence file that contributes to the implementation understanding for this page.
- `packages/core/src/social-providers/atlassian.ts` - Evidence file that contributes to the implementation understanding for this page.
- `packages/core/src/social-providers/cognito.ts` - Evidence file that contributes to the implementation understanding for this page.
- `packages/core/src/social-providers/discord.ts` - Evidence file that contributes to the implementation understanding for this page.
- `packages/core/src/social-providers/dropbox.ts` - Evidence file that contributes to the implementation understanding for this page.
- `packages/core/src/social-providers/facebook.ts` - Evidence file that contributes to the implementation understanding for this page.
- `packages/core/src/social-providers/railway.ts` - Evidence file that contributes to the implementation understanding for this page.

### Source Details
- `packages/cli/src/commands/ai.ts`: language TypeScript; exports `ai`; env `APPDATA`; flags `--args`, `--capabilities`, `--help`, `--provider`, `--registry-url`, `--scope`
- `packages/cli/src/commands/init/configs/social-providers.config.ts`: language TypeScript; exports `ProviderConfig`, `ProviderOption`, `SOCIAL_PROVIDER_CONFIGS`, `SOCIAL_PROVIDERS`, `SocialProvider`
- `packages/core/src/oauth2/oauth-provider.ts`: language TypeScript; exports `OAuth2Tokens`, `OAuth2UserInfo`, `OAuthProvider`, `ProviderOptions`
- `packages/core/src/social-providers/apple.ts`: language TypeScript; exports `apple`, `AppleNonConformUser`, `AppleOptions`, `AppleProfile`, `getApplePublicKey`
- `packages/core/src/social-providers/atlassian.ts`: language TypeScript; exports `atlassian`, `AtlassianOptions`, `AtlassianProfile`
- `packages/core/src/social-providers/cognito.ts`: language TypeScript; exports `cognito`, `CognitoOptions`, `CognitoProfile`, `getCognitoPublicKey`
- `packages/core/src/social-providers/discord.ts`: language TypeScript; exports `discord`, `DiscordOptions`, `DiscordProfile`
- `packages/core/src/social-providers/dropbox.ts`: language TypeScript; exports `dropbox`, `DropboxOptions`, `DropboxProfile`

### Evidence
- `packages/cli/src/commands/ai.ts`
- `packages/cli/src/commands/init/configs/social-providers.config.ts`
- `packages/core/src/oauth2/oauth-provider.ts`
- `packages/core/src/social-providers/apple.ts`
- `packages/core/src/social-providers/atlassian.ts`
- `packages/core/src/social-providers/cognito.ts`
- `packages/core/src/social-providers/discord.ts`
- `packages/core/src/social-providers/dropbox.ts`
- `packages/core/src/social-providers/facebook.ts`
- `packages/core/src/social-providers/railway.ts`
- `packages/core/src/social-providers/vercel.ts`
- `packages/oauth-provider/package.json`
